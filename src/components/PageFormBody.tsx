import styled from 'styled-components'
import tw from 'twin.macro'

export const PageFormBody = styled.div`
  ${tw`h-full overflow-auto`}
  min-width: 37.5rem;
`

export const FormItems = styled.div`
  height: 100%;
`
